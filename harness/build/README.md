# Local build output

Use this directory for temporary test output, logs, and other generated artifacts.
Everything except this README is ignored. Keep secrets out of artifacts even here.
Before committing a build, copy durable redacted result summaries into
`../execution-log.md` and attach necessary evidence to the PR. Local output alone
is not a durable verification record. Build contracts are in `../builds/`.
